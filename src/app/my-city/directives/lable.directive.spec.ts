import { LableDirective } from './lable.directive';

describe('LableDirective', () => {
  it('should create an instance', () => {
    const directive = new LableDirective();
    expect(directive).toBeTruthy();
  });
});
